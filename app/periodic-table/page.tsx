import { ComingSoon } from "@/components/shell/ComingSoon";

export default function PeriodicTablePage() {
  return (
    <ComingSoon
      eyebrow="Module 3 · 3D Visualization & Periodic Table"
      title="Periodic Table"
      description="An interactive, faithfully laid-out IUPAC periodic table — with electron configurations, periodic-trend heat-maps, and 3D atomic models for every element — is coming. It will also feed the planned Element Shelf in the Sandbox."
    />
  );
}
