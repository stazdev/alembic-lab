import { ComingSoon } from "@/components/shell/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      eyebrow="Alembic · Preferences"
      title="Settings"
      description="Theme, measurement units, accessibility options, and lab preferences will live here. For now, the app runs on sensible defaults."
    />
  );
}
