import { ComingSoon } from "@/components/features/shell/ComingSoon";

export default function ProfilePage() {
  return (
    <ComingSoon
      eyebrow="Alembic · Account"
      title="Profile"
      description="Your account, saved experiments, and progress across modules will live here once accounts are wired up."
    />
  );
}
